package com.nd.enums;

public enum WantlistRemovalReason {

      YES,               // Fulfilled through the platform
        NO,                // Not fulfilled through the platform
        NO_LONGER_NEEDED   // User no longer needed the item

}
